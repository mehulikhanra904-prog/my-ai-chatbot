{
  "name": "my-ai-chatbot",
  "private": true,
  "workspaces": [
    "backened",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --prefix backened run dev\" \"npm --prefix frontend run dev\"",
    "start": "npm run dev",
    "backend": "npm --prefix backened run dev",
    "frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "js": "^0.1.0",
    "jsonwebtoken": "^9.0.3"
  }
}
